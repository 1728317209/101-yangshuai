
import { init_state } from './INIT_STATE';

export default function itemMessages(state=init_state, action) {
    // switch (action.type) {
    //     case value:
            
    //         break;
    
    //     default:
    //         return state;
    // }
    return state;

}